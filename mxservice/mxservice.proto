syntax = 'proto3';

option go_package = "example.com/MutualExclusion;mxservice";

package mxservice;

service mutualEX {
  rpc WriteToLog(Message) returns (Empty);
}

message Message {
  int64 timestamp = 1;
  string text = 2;
}

message Empty {}